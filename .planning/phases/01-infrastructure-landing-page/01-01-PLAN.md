---
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/index.css
  - frontend/index.html
  - frontend/src/App.jsx
  - frontend/src/pages/LandingPage.jsx
  - frontend/src/components/ThreeCanvas.jsx
autonomous: true
---

# Plan 01-01: Update configs and build Landing Page

## Goal
Set up the Monotone Tailwind configuration in the existing `frontend` directory and build the Landing Page with the 3D Canvas.

## Requirements
- WEB-01
- WEB-02

## Tasks

```xml
<task>
  <action>
    Install `three` dependency by running `npm install three` in the `frontend` directory.
  </action>
  <read_first>
    - frontend/package.json
  </read_first>
  <acceptance_criteria>
    - `frontend/package.json` contains `"three"` in dependencies.
  </acceptance_criteria>
</task>

<task>
  <action>
    Update `frontend/index.html` to include Google Fonts (Inter: 400 to 800) and Material Symbols Outlined (FILL 0..1, wght 100..700). Also add `<div id="three-canvas-container"></div>` right after `<body>`.
  </action>
  <read_first>
    - frontend/index.html
    - design/Text.txt
  </read_first>
  <acceptance_criteria>
    - `frontend/index.html` contains `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@...`
    - `frontend/index.html` contains `<div id="three-canvas-container"></div>`.
  </acceptance_criteria>
</task>

<task>
  <action>
    Overwrite `frontend/src/index.css` with the Tailwind v4 `@theme` configuration mapping the Monotone colors from `design/Text.txt`.
    Set:
    `--color-background`: `#0a0a0a`
    `--color-surface`: `#121212`
    `--color-on-background`: `#e3e2e2`
    `--color-on-surface`: `#e3e2e2`
    `--color-primary`: `#ffffff`
    `--color-secondary`: `#c7c6c6`
    `--color-tertiary`: `#e3e2e2`
    `--color-outline`: `#404040`
    And override basic standard styling for body padding/margin per `design/Text.txt`.
  </action>
  <read_first>
    - frontend/src/index.css
    - design/Text.txt
  </read_first>
  <acceptance_criteria>
    - `frontend/src/index.css` contains `@theme { --color-background: #0a0a0a; ... }` (or similar valid v4 map).
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `frontend/src/components/ThreeCanvas.jsx` to load the exact vanilla Three.js script snippet from `design/Text.txt`. It should use a `useEffect` that initializes the scene and binds `renderer.domElement` to the existing `<div id="three-canvas-container"></div>` or a newly created ref. Import `* as THREE from 'three'`.
  </action>
  <read_first>
    - design/Text.txt
  </read_first>
  <acceptance_criteria>
    - `frontend/src/components/ThreeCanvas.jsx` contains `import * as THREE from 'three'` and standard React component structure.
  </acceptance_criteria>
</task>

<task>
  <action>
    Create `frontend/src/pages/LandingPage.jsx`. Replicate the HTML from `design/Text.txt` for the Landing page (Nav, Hero, About, Process, Footer). Place `<ThreeCanvas />` in this page so it renders the background.
  </action>
  <read_first>
    - design/Text.txt
    - frontend/src/components/ThreeCanvas.jsx
  </read_first>
  <acceptance_criteria>
    - `frontend/src/pages/LandingPage.jsx` exports `LandingPage` component and renders the exact classes for Nav, Hero, etc. from the design text.
  </acceptance_criteria>
</task>

<task>
  <action>
    Update `frontend/src/App.jsx` routing: Add `<Route path="/" element={<LandingPage />} />` as the root route. Make sure `*` fallback continues to `/login`.
  </action>
  <read_first>
    - frontend/src/App.jsx
  </read_first>
  <acceptance_criteria>
    - `frontend/src/App.jsx` imports `LandingPage` and includes `<Route path="/" element={<LandingPage />} />`.
  </acceptance_criteria>
</task>
```

## Verification
- Run `npm run build` in `frontend/` to verify Type/Syntax checks.
- Visually verify by running `npm run dev` in `frontend/` and checking `http://localhost:5173/`.

## Must Haves
- The Tailwind configuration must exactly match the Monotone styling.
- The 3D animation must run correctly in the background of the landing page.
- Landing page must load on `/` path.
