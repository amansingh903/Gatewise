---
phase: 01-auth-flow-root-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [mobile/app/_layout.tsx]
autonomous: true
requirements: [M-01]
must_haves:
  truths:
    - "Root navigation framework is established with primary routing groups."
  artifacts:
    - path: "mobile/app/_layout.tsx"
      provides: "Root router layout containing (auth), (resident), (guard), (service)"
  key_links: []
---

<objective>
Refactor `app/_layout.tsx` to define the root Stack with `(auth)`, `(resident)`, `(guard)`, and `(service)` groups.

Purpose: Establishes the foundational role-based navigation groups.
Output: Restructured `app/_layout.tsx`.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Root Layout Groups</name>
  <read_first>mobile/app/_layout.tsx</read_first>
  <acceptance_criteria>
    - `mobile/app/_layout.tsx` must contain `<Stack>` with `screenOptions={{ headerShown: false }}`.
    - It must include `<Stack.Screen name="(auth)" />`.
    - It must include `<Stack.Screen name="(resident)" />`.
    - It must include `<Stack.Screen name="(guard)" />`.
    - It must include `<Stack.Screen name="(service)" />`.
  </acceptance_criteria>
  <action>
Rewrite `mobile/app/_layout.tsx` (or update it if it exists). Provide a default export `RootLayout` component returning an Expo Router `<Stack screenOptions={{ headerShown: false }}>` containing screens for the 4 role-based groups: `(auth)`, `(resident)`, `(guard)`, and `(service)`. Use `{ headerShown: false }` for all screens.
  </action>
  <verify>
    <automated>grep -E "(auth)|(resident)|(guard)|(service)" mobile/app/_layout.tsx</automated>
  </verify>
  <done>Root Stack contains the 4 layout paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Interface | Navigation state within the device memory |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01   | E | _layout.tsx | accept | Mock auth state is accepted for Phase 1. Real JWT/RBAC later. |
</threat_model>

<verification>
File is correctly formatted and exports the Stack.
</verification>

<success_criteria>
Root Stack layout defines the four primary application groups.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-flow-root-routing/01-01-SUMMARY.md`
</output>
