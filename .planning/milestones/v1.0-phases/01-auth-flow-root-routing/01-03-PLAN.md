---
phase: 01-auth-flow-root-routing
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [mobile/app/(auth)/index.tsx, mobile/components/ui/RoleCard.tsx]
autonomous: true
requirements: [M-01, M-02]
must_haves:
  truths:
    - "Users see 3 role selection options upon opening the app."
    - "UI aesthetic matches Stitch Gunmetal + Aqua design."
  artifacts:
    - path: "mobile/app/(auth)/index.tsx"
      provides: "Role Selection screen"
  key_links: []
---

<objective>
Create `app/(auth)/index.tsx` — Role Selection screen based on Stitch "Select Role (Streamlined)".

Purpose: Implements the portal entry showing the 3 glass role cards on a gunmetal obsidian background.
Output: Index auth screen and reusable RoleCard UI component.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement RoleCard component</name>
  <read_first>mobile/app/(auth)/_layout.tsx</read_first>
  <acceptance_criteria>
    - `mobile/components/ui/RoleCard.tsx` created.
    - Card has `surface-container-high` (`#25293A`) styling and border/blur effects matching glassmorphism.
  </acceptance_criteria>
  <action>
Create a reusable `RoleCard` component in `mobile/components/ui/RoleCard.tsx`. It needs props for `title` (string), `icon` (string/ReactNode), and `onPress` callback. Use Tailwind / NativeWind for styling: dark slate background (`bg-[#25293A]`), subtle borders (`border-white/10`), rounded corners (`rounded-xl`), padding, and active/focus states via `active:scale-95` to make it interactive.
  </action>
  <verify>
    <automated>test -f mobile/components/ui/RoleCard.tsx</automated>
  </verify>
  <done>Component created and tailored for glassmorphism.</done>
</task>

<task type="auto">
  <name>Task 2: Build Role Selection Screen</name>
  <read_first>mobile/components/ui/RoleCard.tsx</read_first>
  <acceptance_criteria>
    - `mobile/app/(auth)/index.tsx` created.
    - Uses dark background (`#111418`).
    - Maps 3 RoleCards ("Resident", "Security Guard", "Service Staff").
  </acceptance_criteria>
  <action>
Create `mobile/app/(auth)/index.tsx` acting as the initial entry point. Make the `View` `<View className="flex-1 bg-[#111418] p-6 justify-center">`. Display the header "GateWise Access Portal" with white text and `font-bold`. Render three `RoleCard` components for:
1. "Resident"
2. "Security Guard"
3. "Service Staff"
Add a dummy `onPress` pushing to `/login?role=X` via `require('expo-router').useRouter()`.
  </action>
  <verify>
    <automated>test -f mobile/app/\(auth\)/index.tsx && grep -q "GateWise Access Portal" mobile/app/\(auth\)/index.tsx</automated>
  </verify>
  <done>Screen looks like Stitch UI specification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client | UI purely visual at this stage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03   | S | (auth)/index.tsx | accept | Static page, no dynamic injections possible |
</threat_model>

<verification>
File exists and exports correctly.
</verification>

<success_criteria>
Roles screen mirrors exactly the provided Stitch "Select Role" design requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-flow-root-routing/01-03-SUMMARY.md`
</output>
