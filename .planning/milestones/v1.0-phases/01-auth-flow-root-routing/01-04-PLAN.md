---
phase: 01-auth-flow-root-routing
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [mobile/app/(auth)/login.tsx, mobile/components/ui/GlassInput.tsx]
autonomous: true
requirements: [M-02]
must_haves:
  truths:
    - "Users can input their email and password."
    - "Login inputs employ glassmorphism aesthetics."
  artifacts:
    - path: "mobile/app/(auth)/login.tsx"
      provides: "Login authentication screen"
  key_links: []
---

<objective>
Create `app/(auth)/login.tsx` — Login screen based on Stitch "Login (with Back)".

Purpose: Provide a unified logic screen styled in gunmetal for logging in.
Output: Login Auth screen.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build GlassInput component</name>
  <read_first>mobile/app/(auth)/index.tsx</read_first>
  <acceptance_criteria>
    - `mobile/components/ui/GlassInput.tsx` created.
    - TextInput utilizes translucent styles with white text color.
  </acceptance_criteria>
  <action>
Create `mobile/components/ui/GlassInput.tsx`. Export a React Native `TextInput` wrapped in a `<View>` serving as a glass container: `bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white placeholder-gray-400`. Pass down remaining TextInput props securely.
  </action>
  <verify>
    <automated>test -f mobile/components/ui/GlassInput.tsx</automated>
  </verify>
  <done>GlassInput is reusable across forms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Login Screen</name>
  <read_first>mobile/components/ui/GlassInput.tsx</read_first>
  <acceptance_criteria>
    - `mobile/app/(auth)/login.tsx` created.
    - Contains native back button structure pointing to Role Selection.
    - Features login CTA with Aqua (`#53FEC2`) branding.
  </acceptance_criteria>
  <action>
Create `mobile/app/(auth)/login.tsx`. Read the passed `role` query param using `useLocalSearchParams()`. Display dynamic title "(Role) Login" or "Return to Portal". Use `GlassInput` for "Email" and "Password" fields. Make a primary "Sign In" button with Aqua (`bg-[#53FEC2]`) and dark text. Implement a Back icon/button to use `router.back()` or `router.navigate('/')`. 
  </action>
  <verify>
    <automated>test -f mobile/app/\(auth\)/login.tsx && grep -q "login" mobile/app/\(auth\)/login.tsx</automated>
  </verify>
  <done>Login screen mirrors exact design requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Input | User supplies credentials |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04   | I | login.tsx | mitigate | Use secureTextEntry for password field to obscure characters. |
</threat_model>

<verification>
File loads visually perfectly along with logic stubs.
</verification>

<success_criteria>
Login screen implements the Stitch layout effectively.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-flow-root-routing/01-04-SUMMARY.md`
</output>
