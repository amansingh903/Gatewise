---
phase: 01-auth-flow-root-routing
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [mobile/app/(auth)/_layout.tsx]
autonomous: true
requirements: [M-01]
must_haves:
  truths:
    - "Auth routing group has an active layout"
  artifacts:
    - path: "mobile/app/(auth)/_layout.tsx"
      provides: "Layout config for the auth group"
  key_links: []
---

<objective>
Create `app/(auth)/_layout.tsx` with a Stack layout and `headerShown: false`.

Purpose: Provides the scaffolding for Role Selection and Login pages.
Output: Initialized Stack layout for unauthenticated flows.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Auth Layout</name>
  <read_first>mobile/app/_layout.tsx</read_first>
  <acceptance_criteria>
    - `mobile/app/(auth)/_layout.tsx` must be created.
    - Stack must be exported with `headerShown: false` as default screen options.
  </acceptance_criteria>
  <action>
Create `mobile/app/(auth)/_layout.tsx`. Export a default function returning `<Stack screenOptions={{ headerShown: false }}>`. This allows the auth screens to render cleanly without native headers interfering with our custom UI.
  </action>
  <verify>
    <automated>test -f mobile/app/\(auth\)/_layout.tsx && grep -q "headerShown: false" mobile/app/\(auth\)/_layout.tsx</automated>
  </verify>
  <done>Layout file exists and hides headers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Auth Flow | Presenting login screen, no auth boundaries crossed yet |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02   | S | (auth)/_layout.tsx | mitigate | Keep header hidden so headers cannot be manipulated or spoofed by platform. |
</threat_model>

<verification>
File is evaluated by the compiler perfectly.
</verification>

<success_criteria>
(auth) navigation group layout exists and functions correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-flow-root-routing/01-02-SUMMARY.md`
</output>
