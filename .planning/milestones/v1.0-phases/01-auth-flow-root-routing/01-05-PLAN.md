---
phase: 01-auth-flow-root-routing
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified: [mobile/app/(resident)/_layout.tsx, mobile/app/(guard)/_layout.tsx, mobile/app/(service)/_layout.tsx, mobile/app/(auth)/login.tsx]
autonomous: true
requirements: [M-03]
must_haves:
  truths:
    - "Successful login dynamically moves user to the associated layout route block."
  artifacts:
    - path: "mobile/app/(resident)/_layout.tsx"
      provides: "Stubbed layout for Resident role"
  key_links:
    - from: "mobile/app/(auth)/login.tsx"
      to: "/(resident)"
      via: "router.replace"
      pattern: "router\\.replace"
---

<objective>
Wire navigation flow — complete the full chain from role selection → login → tab group routing. 

Purpose: Ties Phase 1 together ensuring the app can correctly navigate between auth blocks and secure blocks using roles.
Output: Working navigational transitions and dummy protected routes.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md
@mobile/app/(auth)/login.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Role Layout Stubs</name>
  <read_first>mobile/app/_layout.tsx</read_first>
  <acceptance_criteria>
    - `mobile/app/(resident)/_layout.tsx` created.
    - `mobile/app/(guard)/_layout.tsx` created.
    - `mobile/app/(service)/_layout.tsx` created.
  </acceptance_criteria>
  <action>
Create minimal dummy Layout files (`_layout.tsx`) in `mobile/app/(resident)`, `mobile/app/(guard)`, and `mobile/app/(service)`. They should simply export an Expo Router `<Stack>` or `<Slot>` wrapped in an abstract generic View containing text like "Resident App Placeholder". They act merely as route test targets for this phase.
  </action>
  <verify>
    <automated>test -f mobile/app/\(resident\)/_layout.tsx && test -f mobile/app/\(guard\)/_layout.tsx && test -f mobile/app/\(service\)/_layout.tsx</automated>
  </verify>
  <done>Layout routes successfully resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Conditional Success Routing</name>
  <read_first>mobile/app/(auth)/login.tsx</read_first>
  <acceptance_criteria>
    - Login "Sign In" button executes a dynamic route shift.
    - Uses `router.replace` to navigate effectively without backwards leaks.
  </acceptance_criteria>
  <action>
Modify `mobile/app/(auth)/login.tsx`. In the submit function stub, check the `role` obtained from layout props or params (e.g. `'resident'`, `'guard'`, `'service'`). Depending on the role, trigger `router.replace('/(resident)')`, `router.replace('/(guard)')`, or `router.replace('/(service)')`.
  </action>
  <verify>
    <automated>grep -E "router\.replace\('\/\(resident\)'\)" mobile/app/\(auth\)/login.tsx</automated>
  </verify>
  <done>Login success successfully points users to target destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Routing | Navigation access enforcement without APIs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05   | E | login.tsx | accept | Auth flows are mock logic routing for layout tests. Protected state enforced in subsequent phase. |
</threat_model>

<verification>
App flawlessly navigates from selection, to auth, to secure screens inside simulator without type crashes.
</verification>

<success_criteria>
Navigational pathways are permanently verified and closed for Phase 1. 
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-flow-root-routing/01-05-SUMMARY.md`
</output>
