---
phase: 02-resident-app
plan: 05
title: Alerts & Notifications Screen
wave: 2
autonomous: true
files_modified:
  - mobile/app/(resident)/messages.tsx
requirements:
  - M-07
---

<objective>
Build `app/(resident)/messages.tsx` — the Alerts & Notifications screen matching Stitch "Alerts (Gunmetal)" HTML. Features: a high-priority entry request action card with Approve/Deny buttons, a scrollable activity feed with person cards and badges, and a simulated live camera card.
</objective>

<context>
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md — Design system (Emergency Alert Hero Card, Person Card, Status Badge sections)
@mobile/components/ResidentHeader.tsx — Header component
</context>

<tasks>

## Task 1: Entry Request Action Card

Replace the placeholder in `mobile/app/(resident)/messages.tsx`:

- Background `#090E18` with LinearGradient
- Import and render `<ResidentHeader />`
- Section label "PENDING ACTIONS" (10px, uppercase, `#53FEC2`, letterSpacing 3)
- **Featured Entry Request Card** (elevated glass card, borderRadius 24):
  - Top: "Entry Request" badge (amber/warning style, `#FACC15` text)
  - Visitor info: name "Rajesh Kumar", subtitle "Delivery — Amazon Package"
  - Timestamp: "Arrived 3 min ago"
  - Vehicle info: "White Van • DL-7C-1234"
  - **Action buttons row**:
    - "Approve" button: bg `#25E0A7`, text `#090E18`, borderRadius 12, height 44
    - "Deny" button: bg `rgba(238,125,119,0.15)`, text `#EE7D77`, border `rgba(238,125,119,0.3)`, borderRadius 12
  - 80×80 camera thumbnail placeholder (dark bg, rounded corner) on right side

### Verification
- [ ] Entry request card renders with all elements
- [ ] Approve/Deny buttons are tappable and styled correctly

## Task 2: Activity Feed + Camera Card

Below the action card:

- Section label "ACTIVITY HISTORY"
- **Activity feed list** (5 mock items) using Person Card pattern:
  - Each item: glass subtle background on hover, flexDirection row
  - 40×40 avatar (rounded-xl, bg `#25293A`)
  - Name, action text ("Entered via Gate A"), timestamp
  - Right side: directional badge "Inbound" (accent style) or "Outbound" (neutral)
- **Live Camera Card** at bottom:
  - Glass card, borderRadius 20
  - Dark placeholder (160px height) simulating camera feed
  - Camera label "Gate A — Live Feed"
  - Green dot + "Online" status
  - Tap hint: "Tap to expand"

### Verification
- [ ] 5 activity feed items render
- [ ] Inbound/Outbound badges have correct colors
- [ ] Camera card renders
- [ ] `tsc --noEmit` passes

</tasks>

<success_criteria>
- [ ] Entry request action card with Approve/Deny
- [ ] Activity feed with 5 items and directional badges
- [ ] Camera card placeholder
- [ ] tsc --noEmit passes
</success_criteria>
