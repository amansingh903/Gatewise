---
phase: 02-resident-app
plan: 03
title: Access Management Screen
wave: 2
autonomous: true
files_modified:
  - mobile/app/(resident)/visitors.tsx
requirements:
  - M-05
---

<objective>
Build `app/(resident)/visitors.tsx` — the Access Management screen matching Stitch "Access Management (Aqua)" HTML. Features pill tabs (Regular Guests / Household Staff), person cards with schedule text and access toggle switches, and a teal FAB that navigates to Create Pass.
</objective>

<context>
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md — Design system (Toggle Switch, Person Card, FAB sections)
@mobile/components/ResidentHeader.tsx — Header component
</context>

<tasks>

## Task 1: Pill Tabs + Person Cards

Replace the placeholder in `mobile/app/(resident)/visitors.tsx`:

- Background `#090E18` with LinearGradient
- Import and render `<ResidentHeader />`
- **Hero heading**: "Access Management" (Display size 32px, Inter-Bold, `#DEE1F7`)
- **Pill Tabs row**: Two pills — "Regular Guests" (active) and "Household Staff" (inactive)
  - Active: bg `#34495E`, text `#DBE5FF`, rounded-full
  - Inactive: bg transparent, text `#9BABCE`, border `rgba(255,255,255,0.1)`
  - Use React state to toggle active tab
- **Person Cards list** (mock data, 4-5 items per tab):
  - Each card: glass card background, flexDirection row
  - 56×56 avatar placeholder (rounded-full, bg `#25293A`)
  - Name (15px, semibold, `#DEE1F7`), subtitle schedule ("Mon, Wed, Fri • 9 AM – 6 PM", 12px, `#9BABCE`)
  - Right side: Toggle Switch per UI spec (44×24, accent `#25E0A7` when on, outline when off, animated thumb)

### Verification
- [ ] Tab switching works between Regular Guests and Household Staff
- [ ] Person cards render with avatars and text
- [ ] Toggle switches animate between on/off states

## Task 2: Floating Action Button

Add the teal FAB at bottom-right:

- Position absolute, bottom 100 (above tab bar), right 20
- Size 64×64, borderRadius 16
- Background `#25E0A7`
- Icon: `add` (MaterialIcons, 30px, color `#090E18`)
- Shadow: `0 25px 50px rgba(37,224,167,0.2)`
- onPress: `router.push('/(resident)/create-pass')`
- Active press: scale down to 0.9 using Animated.spring

### Verification
- [ ] FAB renders above tab bar
- [ ] FAB navigates to create-pass screen
- [ ] Press animation works
- [ ] `tsc --noEmit` passes

</tasks>

<success_criteria>
- [ ] Pill tabs switch between two guest lists
- [ ] Person cards with toggles render correctly
- [ ] Teal FAB visible and navigates to Create Pass
- [ ] tsc --noEmit passes
</success_criteria>
