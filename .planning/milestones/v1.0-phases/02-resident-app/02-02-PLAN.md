---
phase: 02-resident-app
plan: 02
title: Resident Home Dashboard
wave: 2
autonomous: true
files_modified:
  - mobile/app/(resident)/index.tsx
requirements:
  - M-04
  - S-04
---

<objective>
Rebuild `app/(resident)/index.tsx` from the placeholder stub into the full Resident Home Dashboard matching Stitch "Resident Home (Gunmetal)" HTML. Features: sticky ResidentHeader, active pass card with pulsing status dot, quick actions bento grid, recent visitors list, and community notice board.
</objective>

<context>
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md — Full design system
@mobile/components/ResidentHeader.tsx — Header component from Plan 02-01
</context>

<tasks>

## Task 1: Active Pass Card + Pulsing Dot

Replace the placeholder in `mobile/app/(resident)/index.tsx` with the full home screen. Start with:

- ScrollView with `#090E18` background, LinearGradient backdrop
- Import and render `<ResidentHeader />` at top
- **Active Pass Card** (glass card pattern):
  - Background: `rgba(20,25,35,0.6)`, border `rgba(255,255,255,0.1)`, borderRadius 20
  - QR code placeholder (64×64 white rounded rectangle)
  - Pass title "One-Time Delivery Pass", validity "Valid: Today, 2:00 PM – 5:00 PM"
  - Status pill with "Active" text in `#25E0A7`, tiny pulsing green dot using `Animated.loop` with `Animated.sequence([fadeIn, fadeOut])` (requirement S-04)
  - Bottom row: "Tap to view full pass" with chevron-right icon

### Verification
- [ ] Active pass card renders with glass styling
- [ ] Green dot pulses with looping animation

## Task 2: Quick Actions Bento Grid

Below the pass card, add a Bento Grid section:

- Section label "Quick Actions" (10px, uppercase, `#9BABCE`, letterSpacing 3)
- 2-column grid with gap 12:
  - **Primary Card (left, full height)**: Gunmetal surface (`#34495E`), borderRadius 24, height 144. Contains: icon `add_circle_outline`, title "Create One-Time Pass", subtitle "Generate a visitor QR code". Arrow icon bottom-right.
  - **Two stacked secondary cards (right column)**: Glass card pattern, height ~66 each.
    - Card 1: "View All Visitors" with `group` icon
    - Card 2: "Community Board" with `forum` icon

### Verification
- [ ] Bento grid renders with correct 2-column layout
- [ ] Primary card uses Gunmetal (#34495E) background

## Task 3: Recent Visitors List & Community Notice

Below bento grid:

- **Recent Visitors** section:
  - Section label "Recent Visitors"
  - 3 mock visitor items using Person Card pattern:
    - 48×48 avatar placeholder (rounded-2xl, bg `#25293A`)
    - Name (15px, Inter-SemiBold, `#DEE1F7`), subtitle (12px, Inter-Light, `#9BABCE`)
    - Right side: time string ("2:15 PM") or status badge
  - "View All" link at bottom

- **Community Notice** card:
  - Glass card with 24px padding
  - Eye icon + "Notice Board" header
  - Notice text: "Monthly maintenance scheduled for Block A elevators on April 15th."
  - Timestamp: "Posted 2h ago"

### Verification
- [ ] 3 visitor items render with correct styling
- [ ] Community notice card displays
- [ ] Entire screen scrolls smoothly
- [ ] `tsc --noEmit` passes

</tasks>

<success_criteria>
- [ ] Full Home Dashboard renders matching Stitch Gunmetal layout
- [ ] Active pass pulsing dot animates (S-04)
- [ ] Bento grid primary card uses #34495E Gunmetal surface
- [ ] Recent visitors list with 3 items
- [ ] tsc --noEmit passes
</success_criteria>
