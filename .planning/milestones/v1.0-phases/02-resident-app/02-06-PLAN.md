---
phase: 02-resident-app
plan: 06
title: Vault Empty State
wave: 3
autonomous: true
files_modified:
  - mobile/app/(resident)/vault.tsx
requirements:
  - S-01
---

<objective>
Build `app/(resident)/vault.tsx` — a themed empty state placeholder for the Vault tab. No Stitch source exists for this screen, so it must be design-system-consistent using the glass panel pattern with "No data available" copy per the UI Spec copywriting contract.
</objective>

<context>
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md — Design system (Glass Card, Copywriting Contract)
@mobile/components/ResidentHeader.tsx — Header component
</context>

<tasks>

## Task 1: Themed Empty State

Replace the placeholder in `mobile/app/(resident)/vault.tsx`:

- Background `#090E18` with LinearGradient
- Import and render `<ResidentHeader />`
- Centered content area (flex 1, alignItems center, justifyContent center)
- **Glass panel** (300px width, borderRadius 20, glass card pattern):
  - Large icon: `account_balance_wallet` (MaterialIcons, 64px, color `#424753`)
  - Heading: "No data available" (18px, Inter-SemiBold, `#DEE1F7`)
  - Body: "There is no activity found." (14px, Inter-Regular, `#9BABCE`)
  - Subtle lock icon footer: `lock` (12px, `#3e4759`) + "Secure Digital Vault — Coming Soon"
- Consistent padding and spacing per UI spec spacing scale

### Verification
- [ ] Empty state renders centered with glass panel
- [ ] Copy matches UI Spec copywriting contract exactly
- [ ] `tsc --noEmit` passes

</tasks>

<success_criteria>
- [ ] Vault screen shows themed empty state
- [ ] Glass panel style consistent with design system
- [ ] Copy: "No data available" / "There is no activity found."
- [ ] tsc --noEmit passes
</success_criteria>
