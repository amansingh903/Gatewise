---
phase: 02-resident-app
plan: 04
title: Create Pass Form Screen
wave: 3
autonomous: true
files_modified:
  - mobile/app/(resident)/create-pass.tsx
requirements:
  - M-06
---

<objective>
Build `app/(resident)/create-pass.tsx` — the Create Pass form matching Stitch "Create Pass" HTML. Features: glass form sections with light cream inputs, 2×2 access type selector grid (Delivery/Cab/Guest/Service), date/time pickers, multi-entry toggle, and a primary "Generate QR Pass" CTA button.
</objective>

<context>
@.planning/phases/01-auth-flow-root-routing/01-UI-SPEC.md — Design system (Input Fields, Access Type Selector, Toggle Switch sections)
</context>

<tasks>

## Task 1: Form Layout + Light Inputs

Replace the placeholder in `mobile/app/(resident)/create-pass.tsx`:

- Background `#090E18` with LinearGradient
- Back button at top (`router.back()`, "← Back to Visitors")
- Screen title "Create Visitor Pass" (24px, Inter-Bold, `#DEE1F7`)
- **Form section** in a glass card:
  - "Guest Details" section label (10px, uppercase, `#9BABCE`)
  - **Guest Name input**: cream background (`#FAF9F6`), borderRadius 12, padding 14, dark text (`#1a1a2e`), placeholder "Enter guest name"
  - **Phone Number input**: same styling, placeholder "Enter phone number", keyboardType numeric
  - **Flat/Unit input**: same styling, placeholder "A-402" (pre-filled)

### Verification
- [ ] Form renders with light cream inputs on dark background
- [ ] Inputs accept text entry
- [ ] Back button navigates back

## Task 2: Access Type Selector Grid + Options

Below the guest details form:

- "Access Type" section label
- **2×2 grid** of selectable tiles:
  - Delivery: icon `local_shipping`
  - Cab: icon `directions_car`
  - Guest: icon `family_restroom`
  - Service: icon `construction`
- Default tile: cream bg (`#FAF9F6`), text `#64748B`
- Selected tile: bg `#34495E`, border `rgba(100,116,139,0.3)`, text `#DBE5FF`, elevated shadow
- Only one tile selectable at a time (React state)
- Below grid:
  - "Visit Date" and "Visit Time" fields (cream inputs, with calendar/clock icons)
  - "Allow Multiple Entries" row with Toggle Switch (same spec as Access Management)

### Verification
- [ ] Grid renders 4 tiles in 2×2 layout
- [ ] Selection highlights tile with Gunmetal (#34495E) background
- [ ] Toggle switch animates

## Task 3: Generate QR CTA Button

Below all form sections:

- Primary CTA button matching auth screen pattern:
  - Full width, height 52, borderRadius 14
  - LinearGradient background `['#1e3a5f', '#152641']`
  - Text "GENERATE QR PASS" (Inter-SemiBold, 14px, letterSpacing 2, `#DBE5FF`)
  - Icon `qr_code` on left
  - Border: `1px solid rgba(219,229,255,0.15)`
  - onPress: show mock success (Alert or navigate back)
  - Animated press scale (spring to 0.97)

### Verification
- [ ] CTA button renders with gradient background
- [ ] Press animation works
- [ ] `tsc --noEmit` passes

</tasks>

<success_criteria>
- [ ] Form with 3 cream inputs renders
- [ ] 2×2 access type grid with selection state
- [ ] Toggle switch for multi-entry
- [ ] Generate QR Pass CTA with gradient + animation
- [ ] tsc --noEmit passes
</success_criteria>
