# GateWise Mobile

Expo Router app for resident, guard, and service staff workflows.

## Run

```bash
npm install
npx expo start -c
```

The app auto-detects the Expo dev-server host and uses port `5000` for the backend API. To override it:

```env
EXPO_PUBLIC_API_URL=http://YOUR_MACHINE_IP:5000
```
